# Dependências da análise (heatmap) — SÓ para o Jetson, separadas da app de streaming.
# Instalar dentro de um venv próprio (ver README.md) para não mexer no .venv da app.
#
# NOTA IMPORTANTE sobre o PyTorch no Jetson:
#   NÃO instalar torch via pip normal — no Jetson (ARM + CUDA) tem de ser a wheel
#   oficial da NVIDIA para o teu JetPack. Ver README.md (passo 1).
#   Este ficheiro assume que o torch JÁ está instalado.

ultralytics>=8.2,<9      # YOLO (deteção de pessoas) + tracking
numpy>=1.24
opencv-python            # já costuma vir no Jetson; se já existir, o pip ignora
